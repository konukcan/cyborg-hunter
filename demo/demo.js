// demo/demo.js
// Stub. The tour engine — lifecycle helper, onSignal lamp wiring, payload
// assembly, downloads, and preview invocation — lands in the next tasks.
// For now this only confirms the library loaded and logs a version stamp
// as a bug-report aid.
console.log('cyborg-hunter demo · library', (window.CyborgHunter && CyborgHunter.VERSION) || 'unknown');
